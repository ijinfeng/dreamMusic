import 'package:flutter/widgets.dart';

class SongLyricView extends StatelessWidget {
  const SongLyricView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('我是歌词'),
    );
  }
}